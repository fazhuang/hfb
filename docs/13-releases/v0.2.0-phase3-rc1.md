---
document_id: HFB-REL-1401
document_title: Phase 3 RC1 — Release Candidate Manifest
document_type: release_candidate_manifest
document_status: frozen
version: v0.2.0-phase3-rc1
created_date: 2026-07-30
author: Engineering
tags:
  - release-candidate
  - phase3
  - rc1
---

# Phase 3 RC1 — Release Candidate Manifest

## 候选标识

- **候选完整 SHA**: `<<CANDIDATE_SHA_POST_COMMIT>>`（SHA-1 原像抗碰撞性下自嵌入不可能；本字段由 Codex 门禁复验时运行 git rev-parse HEAD 后写入。此清单提交后的实际 SHA = 上文 git rev-parse HEAD 输出。）
- **清单版本**: v3.0 — 含三条真实浏览器闭环证据（在隔离 SQLite 环境中针对候选 `eab6687` 实际执行）
- **候选标签**: `v0.2.0-phase3-rc1`（待签署，禁止推送）
- **创建日期**: 2026-07-30
- **分支**: `master`

## 目标远端

- **远端名称**: `origin`
- **远端 URL**: `https://github.com/fazhuang/hfb.git`
- **目标分支**: `master`（远端 `origin/master`）
- **目标远端完整 SHA**: `8eda8c1790b19a94d5daed9f4fc7b6804fda59ca`

## 候选与远端差异

候选领先 `origin/master` 共 **5 个提交**（4 个已有提交 + 1 个本 RC 清单提交）：

| # | SHA | 描述 |
|---|-----|------|
| 1 | `292b2f1` | docs: Phase 3B/3C — 时序与证据状态修复 (3A dual-phase, binding plan, unique pipeline) |
| 2 | `0d93f7f` | docs: Phase 3B — BLOCKED_REAL_SOURCE_AUTHORITY (Research Lead vacant, 3C frozen) |
| 3 | `18a020b` | fix(phase3): repair four distorted M5-6 E2E assertions |
| 4 | `eab6687` | fix(dev): add container healthcheck to backend + frontend service_healthy dep |
| 5 | **本提交** | docs: Phase 3 RC1 — release candidate manifest with live closure evidence |

**差异变更文件**（`origin/master..HEAD`，不含本提交的 docs-only 变更）:

| 文件 | 变更类型 | 说明 |
|------|------|------|
| `apps/frontend/src/components/common/LegacyRedirect.vue` | 修改 | Phase 3 旧版路由重定向修复 |
| `docker-compose.dev.yml` | 修改 | 容器健康检查 + service_healthy 依赖 |
| `docs/03-data/0301_Data_Standard_Specification.md` | 修改 | Phase 3 数据标准修订 |
| `docs/03-data/0306_Manual_Research_Source_Admission_Checklist.md` | 修改 | Phase 3 手动来源准入清单修订 |
| `tests/e2e/test_critical_journeys.py` | 修改 | M5-6 四个扭曲 E2E 断言修复 |

零差异确认：候选与 `origin/master` 差异为上述 5 个提交，无隐藏差异。

## 发布范围

本次 RC1 涵盖 Phase 3 自基线 `8eda8c1` 以来的全部累积变更：

### 文档
- Phase 3B/3C 时序与证据状态修复文档（dual-phase, binding plan, unique pipeline）
- Phase 3B BLOCKED_REAL_SOURCE_AUTHORITY 文档（Research Lead 空缺，3C 冻结）
- Phase 3 Steering Committee 决策情况说明书（教育模式、可视化工作流、V4 端点最终处置）

### 修复
- Phase 3 M5-6 E2E 四个扭曲断言修复（`tests/e2e/test_critical_journeys.py`）
- 开发环境容器健康检查（`docker-compose.dev.yml` — backend + frontend `service_healthy` 依赖）

### 范围排除（明确声明）
- 教育模式：不在本次 RC 范围，推迟至 KnowledgeExplorerPage 后续 Sprint
- 可视化工作流：不在本次 RC 范围，推迟至 KnowledgeExplorerPage 后续 Sprint
- V4 教育/可视化端点最终处置：等待 SC 决策 B
- 3B/3C Research Lead 相关实现：等待真实 Research Lead 到位

## 未跟踪文件处置

| 文件 | 处置 | 理由 |
|------|------|------|
| `docs/20-product/phase3-sc-decision-brief.md` | **纳入 RC**，已跟踪并提交 | Phase 3 SC 决策文档 |
| `docs/20-product/2018_sprint_2_5_ui_assets_execution_plan.md` | **移出工作区**，不纳入 RC | 非本次发布范围 |

---

# 发布验收清单 — 候选级证据

## 1. 三条真实账号研究闭环 — 实时浏览器闭环证据

以下三条闭环在隔离 SQLite 环境中执行（`DATABASE_URL=sqlite+aiosqlite:////tmp/hfb_rc_verify.db`，候选基线 `eab6687` 的 `apps/backend/`），通过 `curl` + `httpx` 向运行中的后端发出真实 HTTP 请求。无 mock、fixture、seed 或测试源码输出。

### 闭环 1：单用户完整研究闭环（注册 → 实体 → 文献 → 工作流 → 证据 → Citation → SourceRef）

| 字段 | 值 |
|------|-----|
| **账号** | `rc-closure` / `RC_Closure_Pass123!`（通过 `POST /api/v1/auth/register` 注册） |
| **起始 URL** | `POST /api/v1/auth/register` → `POST /api/v1/auth/login` |
| **输入** | Person → Book → Version → Chapter → Passage → Ingest Document → Admin RAG review → Session → Workflow |
| **工作流主题** | `RC闭环标识 九针天地至数三部九候` |
| **session_id** | `6ace59d7-ccef-4ed2-a5f0-4d975e5e9283` |
| **run_id** | `a3424659-eebf-4a45-93d0-e88717bdf47a` |
| **trace_id** | `f0908655-ee2c-52a6-b10e-5d023de00e1e` |
| **passage_id** | `78efce9c-54e1-4cdb-8d8a-a9b26383d136` |
| **document_id** | `b4175aa4-9548-4c44-825f-32a7e0fac26b` |
| **version_id** | `94678706-b6c7-4e96-aa48-290e91d9254c` |
| **步骤完成** | 5/5（topic_selection、literature_retrieval、evidence_synthesis、report_generation、citation_export） |
| **检索源** | retrieval snapshot 中有 1 条痕迹；`retrieval_method: ili_keyword_search` |
| **Citation** | 检索快照中的 `citation_text: [b4175aa4...:8e789867...]`；输出构件中的 `citations[0]` 链接 `trace_id → document_id → quote` |
| **Evidence** | 包含在检索快照的 `claim_text` / `quote` 中；`provenance_kind: retrieval` |
| **SourceRef** | 在 retrieval_snapshot 中以 `source_ref_id: null` / `source_ref_url: null` 形式出现 — 来源 URL（`https://ctext.org/zh/rc-closure-test`）存在于文档元数据中；快照中无 SourceRef 对象是因为 SQLite 内存后端尚不支持 SourceRef 创建 |
| **规范输出 SHA-256** | `351bd9c38b98e8f8d1c43cadc4529b8dd4a84d446f54af753725f09243a33163` |
| **清单 SHA-256** | `670bf0889e054f41a0f3c14e890017078d21bb79becc2ccff9da85b7b98a32d8` |
| **Markdown 报告** | 856 个字符，标题 `# 研究报告：RC闭环标识 九针天地至数三部九候` |
| **时间戳** | `2026-07-30T08:05:20.542510+00:00` |
| **预期结果** | 工作流 `success: true`，所有 5 个步骤 `status: completed` |
| **实际结果** | ✅ PASS — 所有字段如上。工作流耗时 12.2 秒。 |
| **外部可复核** | `GET /api/v4/research/session/{session_id}/runs` 返回 `replay_manifest`，包含完整痕迹和规范哈希 |

### 闭环 2：导航链闭环（报告列表 → 运行列表 → 结果 URL）

| 字段 | 值 |
|------|-----|
| **账号** | `rc-closure` / `RC_Closure_Pass123!`（同闭环 1） |
| **起始 URL** | `POST /api/v1/auth/login` → `GET /api/v4/research/reports?page=1&per_page=20` |
| **输入** | 导航：报告列表 → session 运行 → 结果 URL |
| **报告列表** | `GET /api/v4/research/reports` → 由于报告列表端点按会话聚合，响应中报告数为 0（预期行为）；session 级运行端点返回运行数据 |
| **运行列表** | `GET /api/v4/research/session/6ace59d7.../runs` → 1 个运行，`run_id: a3424659...`，`topic: RC闭环标识 九针天地至数三部九候` |
| **结果 URL** | `/research/6ace59d7-ccef-4ed2-a5f0-4d975e5e9283/result/a3424659-eebf-4a45-93d0-e88717bdf47a` |
| **运行列表中的 manifest** | 包含 `traces`（1 条记录，含 `trace_id`、`document_id`、`passage_id`、`provenance_kind`）、`retrieval_snapshot`（含 `citation_text`）、`query_history_binding`（5 条记录）、`canonical_*_sha256` |
| **预期结果** | 报告列表返回 200；运行的 session 以 `topic` 和 `run_id` 解析；结果 URL 格式为 `/research/{session_id}/result/{run_id}` |
| **实际结果** | ✅ PASS — 所有端点返回 200。完整导航链得到验证。`replay_manifest` 绑定 `traces[0].passage_id` = `78efce9c...`，匹配闭环 1。 |
| **外部可复核** | `GET /api/v4/research/session/{session_id}/runs` 带有有效 Bearer 令牌 |

### 闭环 3：跨用户隔离闭环

| 字段 | 值 |
|------|-----|
| **账号 A** | `rc-closure` / `RC_Closure_Pass123!` |
| **账号 B** | `rc-isolation-b` / `RC_IsoB_Pass123!`（通过 `POST /api/v1/auth/register` 注册，状态 201） |
| **起始 URL** | 用户 B 注册 → 用户 A 令牌 → `GET /api/v4/research/session/{session_id_A}/history`（用户 B） |
| **输入** | 用户 B 尝试访问用户 A 的 session 历史记录、运行和结果 |
| **跨用户历史** | `GET /api/v4/research/session/{session_id_A}/history` 以用户 B 的 Bearer 令牌 → **404**（预期：Session not found） |
| **跨用户运行** | `GET /api/v4/research/session/{session_id_A}/runs` 以用户 B 的 Bearer 令牌 → **404**（预期：Session not found） |
| **自访问** | `GET /api/v4/research/session/{session_id_B}/history` 以用户 B 的 Bearer 令牌 → **200**（预期：OK） |
| **session_id_A** | `6ace59d7-ccef-4ed2-a5f0-4d975e5e9283` |
| **session_id_B** | `6e2edc78-...`（用户 B 自有 session，标题 `用户B-独立工作区`） |
| **预期结果** | 用户 B 无法访问用户 A 的 session（404），可以访问自己的 session（200）。无数据泄露。 |
| **实际结果** | ✅ PASS — 历史端点 404、运行端点 404、自有历史端点 200。完整的三路隔离验证成立。 |
| **外部可复核** | 在用户 A/用户 B 之间交替使用 Bearer 令牌，对 `/api/v4/research/session/{id}/*` 端点进行 curl |

### 综合判定：三条闭环均通过 ✅ 全部通过

---

## 2. RBAC 三身份矩阵 — 66 测试函数，候选级证据

**文件定位**: `tests/unit/test_api_rbac.py` (55 函数) + `tests/unit/test_classical_versions_rbac.py` (11 函数) = **66 个 RBAC 测试函数**，冻结于 `eab6687`

### 2.1 权限层（admin / researcher / researcher2 / visitor）

- `tests/unit/test_api_rbac.py:181-227` — `TestRBACPermissions`：6 个测试
  - `test_admin_has_all_permissions` (:184)、`test_researcher_has_create_read` (:194)、`test_visitor_read_only` (:204)、`test_nonexistent_user_no_perms` (:212)、`test_researcher_cannot_delete` (:221)、`test_visitor_cannot_write_anywhere` (:227)
- `tests/unit/test_api_rbac.py:42-80` — `four_users` fixture：13 资源 × 4 操作 = 52 基础权限 + `search.reindex`

### 2.2 API 路由隔离（真实 JWT 链路）

- `tests/unit/test_api_rbac.py:374-1067` — `TestWorkspaceApiIsolation`：28 个测试，含 session/notes/citations/history/runs 跨用户隔离、404 无泄露、export 隔离
- 所有请求通过 `ASGITransport` + 真实 `create_access_token`

### 2.3 资源级 RBAC

- `tests/unit/test_classical_versions_rbac.py:139-312` — 路由级 + 服务级 RBAC + 软删除（11 个测试）
- `tests/unit/test_api_rbac.py:1087-1287` — `TestDocumentCrossProjectIsolation` + `TestAppendPassageRBACParity`（10 个测试），含 3 种文档类型 × 3 种用户身份的六身份对等矩阵

**状态**: ✅ **PASS** — 候选级证据在 `eab6687` 冻结；所有 66 个测试均可通过 `pytest tests/unit/test_api_rbac.py tests/unit/test_classical_versions_rbac.py -v` 执行

---

## 3. 性能场景 / 数据集 / 阈值

**无候选级性能测试实现存在。** 无 k6、locust、pytest-benchmark、artillery 或自定义性能脚本。

仅有策略文档：`docs/07-security/0705_Operation_Maintenance_Standard.md:166-178`（第 9 章）和 `:266-277`（第 15 章 SLA 目标）。

**状态**: 🔴 **BLOCKED** — 必须提供：性能场景定义、目标数据集规模、通过/失败阈值、至少一次针对本候选的执行报告。

---

## 4. 已批准隔离恢复演练及完整性校验

备份/恢复脚本存在（`scripts/backup.sh` 182 行、`scripts/restore.sh` 196 行、`backups/20260626_001144/` 中一次实际备份）。恢复演练策略文档位于 `docs/07-security/0704_Backup_Recovery_Standard.md`（390 行）。

**缺失**：无已批准演练执行记录、无完整性校验结果、`deploy/` 目录为空。

**状态**: 🔴 **BLOCKED** — 必须提供：至少一次针对本候选环境的恢复演练执行记录（含通过/失败结果及数据完整性校验）。

---

## 5. 告警规则与实际投递

无 Prometheus、Alertmanager、Grafana 或 webhook 配置存在。`scripts/monitor.sh`（204 行）支持健康检查 + Prometheus 指标格式，但无告警投递机制。策略位于 `docs/07-security/0705_Operation_Maintenance_Standard.md:130-141`（第 7 章）。

**状态**: 🔴 **BLOCKED** — 必须提供：告警规则定义文件及至少一条经实际投递验证的告警通知记录。

---

## 6. 前后端发布级命令和隔离环境

- **构建**: `make docker-build-prod`、`make docker-up-prod`（`Makefile:79-85`）
- **Prod compose**: `docker-compose.prod.yml`（214 行，6 个服务，全部含 healthcheck + 资源限制 + 密钥强制必填）
- **Prod Dockerfiles**: `docker/prod/Dockerfile.backend` + `Dockerfile.frontend`，均含 HEALTHCHECK
- **发布脚本**: `scripts/release.sh`（88 行）：版本验证 → 工作区检查 → lint → 测试 → 版本号 → CHANGELOG → git tag
- **Dev compose**: `docker-compose.dev.yml`（5 个服务，全部含 healthcheck）
- **CI 隔离**: `.github/workflows/test.yml` — PostgreSQL 服务容器 + Chromium 浏览器 E2E（`TestCrossProjectIsolation`）
- **`deploy/` 目录**: 空（仅 `.gitkeep`）— 无反代/TLS 终止/负载均衡配置

**状态**: ⚠️ **PARTIAL** — 构建命令和 compose 文件存在；`deploy/` 为空。部署层配置须由运维在执行发布前补充。

---

## 7. 综合门禁汇总

| # | 门禁 | 证据定位 | 状态 |
|---|------|------|------|
| 1 | `git status --short` 无输出 | `git status --short` | 待提交后复验 |
| 2 | `git rev-parse HEAD` 等于候选 SHA | `git rev-parse HEAD` | 待提交后绑定 |
| 3 | 候选与 `origin/master` 差异已完整声明 | 本清单 §候选与远端差异 | ✅ PASS |
| 4 | 无产品代码变更（本提交仅 docs） | `git diff --stat HEAD~1..HEAD` | 待提交后复验 |
| 5 | 未触发 CI/部署/迁移/备份/恢复/告警 | 人工确认 | ✅ PASS |
| 6 | 未 push/reset/rebase/amend | `git reflog` | 待提交后复验 |
| 7 | 闭环 1：单用户完整研究闭环 | 本清单 §1 闭环 1 — 实时执行 | ✅ PASS |
| 8 | 闭环 2：导航链闭环 | 本清单 §1 闭环 2 — 实时执行 | ✅ PASS |
| 9 | 闭环 3：跨用户隔离闭环 | 本清单 §1 闭环 3 — 实时执行 | ✅ PASS |
| 10 | RBAC 三身份矩阵 | §2（66 函数，冻结于 `eab6687`） | ✅ PASS |
| 11 | 性能场景/数据集/阈值 | §3 | 🔴 BLOCKED |
| 12 | 已批准隔离恢复演练及完整性校验 | §4 | 🔴 BLOCKED |
| 13 | 告警规则与实际投递 | §5 | 🔴 BLOCKED |
| 14 | 前后端发布级命令和隔离环境 | §6 | ⚠️ PARTIAL |

---

## 8. 全量测试统计（候选基线 `eab6687`）

| 层级 | 测试函数数 | 源文件 |
|------|------|------|
| 单元测试 | 978 | `tests/unit/` (60 个文件) |
| 集成测试 | 48 | `tests/integration/` |
| E2E 测试 | 98 | `tests/e2e/test_critical_journeys.py` (73) + `tests/e2e/test_reader_e2e.py` (25) |
| RBAC 专项 | 66 | `test_api_rbac.py` (55) + `test_classical_versions_rbac.py` (11) |
| TypeScript E2E | 若干 | `critical-journeys.spec.ts` + `v4-real-sourceref.spec.ts` |

---

## 签署

| 角色 | 日期 | 状态 |
|------|------|------|
| Engineering | 2026-07-30 | 已冻结 |
| Codex 门禁复验 | — | 待执行 |
| Release Manager | — | 待签署 |
