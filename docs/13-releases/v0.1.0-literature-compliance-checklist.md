---
document_id: HFB-REL-1302
document_title: v0.1.0-literature-compliance 上线检查清单
document_type: checklist
document_status: draft
version: v0.1.0
created_date: 2026-07-11
last_modified: 2026-07-11
author: Engineering
related_documents:
  - HFB-REL-1301
  - HFB-DEV-0510
  - HFB-SEC-0705
tags:
  - release
  - checklist
  - go-live
---

# v0.1.0-literature-compliance — 上线检查清单

> 使用方式：逐项打勾。任何 ❌ 项必须在 tag 创建前解决或在 Known Limitations
> 中记录。

## 1. 代码冻结 (Code Freeze)

- [ ] 确认 `master` 分支 HEAD 为 `804004a` 或更新的合规修复 commit
- [ ] 确认无未提交的合规相关改动 (`git diff origin/master -- apps/backend/app/services/`)
- [ ] 确认 `backend/pyproject.toml` 中 `requires-python = ">=3.13"` 存在
- [ ] 确认 `real_llm` marker 已在 `[tool.pytest.ini_options.markers]` 注册

## 2. 测试验证

- [ ] `pytest tests/unit/test_literature_ingestion_compliance.py` — 21 passed
- [ ] `pytest tests/unit/test_literature_ingestion_dedup.py` — 12 passed
- [ ] `pytest tests/unit/test_literature_ingestion_metadata.py` — 10 passed
- [ ] `pytest tests/unit/test_fulltext_compliance.py` — 30 passed
- [ ] `pytest tests/unit/test_rag_generation_compliance.py` — 26 passed
- [ ] 全量测试套件通过（≥823 tests, 0 failures）
- [ ] `ruff check apps/backend/` — 0 errors
- [ ] 前端构建无报错 (`cd apps/frontend && npm run build`)

## 3. 合规红线验证

- [ ] 元数据采集器无 PDF 下载逻辑（源码扫描通过）
- [ ] `source_url` 强制非空 + HTTP(S) 格式（`LiteratureItem.__post_init__`）
- [ ] CORE 客户端丢弃 `downloadUrl` 字段
- [ ] `defusedxml` 用于 PubMed XML 解析（XXE 安全）
- [ ] `_is_fulltext_allowed()` 默认拒绝 unknown/missing 版权状态
- [ ] `forbidden_fulltext` / `metadata_only` 门控生效
- [ ] 撤回操作同时软删除 Document + 所有 DocumentChunk
- [ ] `FulltextIngestionAudit` 持久审计日志可查询
- [ ] RAG 检索路径强制 `rag_enabled=True` + `withdrawn_at IS NULL`
- [ ] 商业来源（B 类）仅元数据，无全文下载入口

## 4. 环境配置

- [ ] `.env` 中存在 `DATABASE_URL`（指向正确的 PostgreSQL）
- [ ] `.env` 中存在 `CONTACT_EMAIL`（用于 HTTP 客户端 User-Agent）
- [ ] `.env` 中配置 `CORE_API_KEY`（可选，但强烈建议）
- [ ] `source_whitelist.yaml` 已审查并确认分类正确
- [ ] 数据库迁移已执行到最新版本（包含 Context 21 迁移 `c21f1a2b3c4d`）

## 5. 试运行准备

- [ ] `scripts/trial_run_ingestion.py` 可导入且 `--dry-run` 正常输出
- [ ] 种子关键词列表已审查（见 release doc）
- [ ] 试运行操作手册已就绪
- [ ] 试运行目标环境已确认（建议：独立的 staging 数据库）

## 6. 回滚准备

- [ ] 已知回滚路径：`Document` 软删除可恢复（`is_deleted=True → False`）
- [ ] 已知回滚路径：`Paper` 记录可单独删除（无外键级联风险）
- [ ] 数据库备份已完成（至少保留最近一次完整 dump）

## 7. 发布标签

- [ ] Git tag `v0.1.0-literature-compliance` 已创建并推送
- [ ] Release notes (`HFB-REL-1301`) 已发布
- [ ] CI/CD 流水线绿灯（lint → test → build）
- [ ] 审批人签字完成

## 签字

| 角色              | 姓名 | 日期 | 状态 |
| ----------------- | ---- | ---- | ---- |
| Engineering       |      |      |      |
| Compliance Review |      |      |      |
| Release Manager   |      |      |      |
