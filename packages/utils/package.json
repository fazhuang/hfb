{
  "name": "@hfb/utils",
  "version": "0.2.0",
  "private": true,
  "description": "Shared utility functions for HFB Platform",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
