{
  "name": "@hfb/config",
  "version": "0.2.0",
  "private": true,
  "description": "Shared configuration for HFB Platform (ESLint, TypeScript, etc.)",
  "scripts": {
    "clean": "rm -rf dist"
  }
}
