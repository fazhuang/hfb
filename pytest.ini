[pytest]
# pytest.ini — extends pyproject.toml [tool.pytest.ini_options]

# Filter warnings
filterwarnings =
    error
    ignore::DeprecationWarning:alembic.*
    ignore::DeprecationWarning:pydantic.*
    ignore::ResourceWarning
